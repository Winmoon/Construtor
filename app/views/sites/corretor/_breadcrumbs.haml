.breadcrumbs.margin-bottom-40
  .container
    %h1.pull-left
      = editable_content "title" do
        = t("pages.#{@page}")
    %ul.pull-right.breadcrumb
      %li
        = link_to "Home", "?p=index"
      %li
        = link_to t("pages.#{@page}"), "?p=#{@page}"
